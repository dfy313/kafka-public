# Kafkaesque

A tiny Kafka-like client (HTTP toy) for learning.

## Install

```bash
# ⚠️ NOTE: Replace `username-kafkaesque` with your own PyPI package name.

python -m pip install -i https://test.pypi.org/simple/ \
  --extra-index-url https://pypi.org/simple \
  username-kafkaesque
```
